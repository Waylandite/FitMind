"""Deprecated: graphs in this package are obsolete placeholders.

The actual intent classification, routing, and workflow orchestration
has moved to:

  services/intent_classifier.py   — keyword + LLM intent classification
  services/intent_router.py       — intent → module routing table
  services/chat_service.py        — sequential service chain orchestration
"""
